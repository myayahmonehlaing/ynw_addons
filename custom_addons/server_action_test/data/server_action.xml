<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">

        <record id="action_mark_contact_verified" model="ir.actions.server">
            <field name="name">Mark Contact Verified</field>
            <field name="model_id" ref="base.model_res_partner"/>
            <field name="state">code</field>
            <field name="code">
                if record:
                    record.write({'x_verified': True})
            </field>
        </record>

    </data>
</odoo>
